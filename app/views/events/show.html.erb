<div class="event">
  <div class="event-info row">
    <div class="col-md-12">
      <%# Выводим произвольную фотку в качестве фона для шапки события %>
      <%= content_tag 'div', '',
                      class: 'event-background',
                      style: "background-image: url(#{asset_path('event.jpg')})"
      %>

      <div class="event-info-inner row">
        <div class="col-md-6">
          <div class="event-description">
            <h1><%= @event.title %></h1>
            <%# Для перевода времени в русский формат используем I18n %>
            <%# Формат при этом используется из гема russian %>
            <h3><%= l @event.datetime, format: :short %></h3>
            <h3>
              <small>
                <%= link_to @event.address, "https://www.google.com/maps/embed/v1/place?key=#{@event.address}" %>
              </small>
            </h3>
            <p><%= @event.description %></p>
          </div>

            <div class="buffer-top">
              <%= link_to 'Изменить', edit_event_path(@event), class: 'btn btn-primary' %>
            </div>
        </div>
      </div>
    </div>
  </div>

</div>
